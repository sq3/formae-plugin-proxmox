amends "pkl:Project"

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.80.1"
  }
}

package {
  name = "proxmox"
  baseUri = "package://hub.platform.engineering/plugins/proxmox/schema/pkl/\(name)/\(name)"
  version = read("VERSION").text.trim()
  packageZipUrl = "https://hub.platform.engineering/plugins/proxmox/schema/pkl/\(name)/\(name)@\(version).zip"
}
